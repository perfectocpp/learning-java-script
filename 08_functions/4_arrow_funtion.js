/* let pow = (number)=>{
    return number * number
}

console.log(pow(2))
 */
// if our parameters has only one

let pow = number => number * number

console.log(pow(2))

//
let greet = () => console.log("hello")

greet()